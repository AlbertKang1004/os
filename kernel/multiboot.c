#include "multiboot.h"

/** multiboot_relocate:
 *  Relocates all physical addresses in the multiboot structure
 *  to their corresponding virtual addresses after paging is enabled
 *
 *  @param mbinfo   Pointer to the multiboot info structure
 */
// TO BE ADDED LATER
